meta {
  name: Get a Scan's Vulnerabilities
  type: http
  seq: 22
}

get {
  url: localhost:8000/api/scans/{{scanID}}/vulnerabilities
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth token}}
}
